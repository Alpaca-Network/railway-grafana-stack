apiVersion: 1

providers:
  - name: 'AI Model Analytics'
    orgId: 1
    folder: 'AI Model Analytics'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/model_performance

  - name: 'Application Log Streams'
    orgId: 1
    folder: 'Application Log Streams'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/loki

  - name: 'Distributed Tracing'
    orgId: 1
    folder: 'Distributed Tracing'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/tempo

  - name: 'AI Provider Intelligence'
    orgId: 1
    folder: 'AI Provider Intelligence'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/provider

  - name: 'Four Golden Signals'
    orgId: 1
    folder: 'Four Golden Signals'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/golden-signals

  - name: 'Infrastructure & Caching'
    orgId: 1
    folder: 'Infrastructure & Caching'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/infrastructure

  - name: 'LLM Streaming Performance'
    orgId: 1
    folder: 'LLM Streaming Performance'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/streaming

  - name: 'Platform Quality Pillars'
    orgId: 1
    folder: 'Platform Quality Pillars'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/reliability

